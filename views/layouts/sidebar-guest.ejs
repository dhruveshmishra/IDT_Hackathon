<!-- LEFT SIDEBAR: GUEST/AUTH PORTAL -->
<nav class="sidebar-nav" id="sidebarNav">
  <!-- Toggle button -->
  <button class="sidebar-toggle" id="sidebarToggle" aria-label="Toggle Sidebar">
    <i class="fa-solid fa-chevron-left"></i>
  </button>

  <!-- Brand -->
  <div class="sidebar-brand">
    <div class="sidebar-brand-logo">R</div>
    <span class="sidebar-brand-text">RentIt</span>
  </div>

  <!-- Guest Info/Welcome -->
  <div class="sidebar-user">
    <div class="sidebar-avatar">
      <i class="fa-solid fa-circle-user"></i>
    </div>
    <div class="sidebar-user-info">
      <div class="sidebar-user-name">Guest User</div>
      <div class="sidebar-user-role">Please Log In</div>
    </div>
  </div>

  <!-- Navigation -->
  <ul class="sidebar-menu">
    <li class="sidebar-section-label">General</li>
    <li>
      <a href="/" class="sidebar-menu-link <%= locals.currentPath === '/' ? 'active' : '' %>" data-label="Home">
        <span class="sidebar-menu-icon"><i class="fa-solid fa-house"></i></span>
        <span class="sidebar-menu-label">Home</span>
      </a>
    </li>
    <li>
      <a href="/items" class="sidebar-menu-link <%= locals.currentPath === '/items' ? 'active' : '' %>" data-label="Browse Items">
        <span class="sidebar-menu-icon"><i class="fa-solid fa-magnifying-glass"></i></span>
        <span class="sidebar-menu-label">Browse Items</span>
      </a>
    </li>

    <li class="sidebar-section-label">Authentication</li>
    <li>
      <a href="/auth/login" class="sidebar-menu-link <%= locals.currentPath === '/auth/login' ? 'active' : '' %>" data-label="Login">
        <span class="sidebar-menu-icon"><i class="fa-solid fa-arrow-right-to-bracket"></i></span>
        <span class="sidebar-menu-label">Sign In</span>
      </a>
    </li>
    <li>
      <a href="/auth/signup" class="sidebar-menu-link <%= locals.currentPath === '/auth/signup' ? 'active' : '' %>" data-label="Sign Up">
        <span class="sidebar-menu-icon"><i class="fa-solid fa-user-plus"></i></span>
        <span class="sidebar-menu-label">Sign Up</span>
      </a>
    </li>
  </ul>
</nav>

<div class="sidebar-overlay" id="sidebarOverlay"></div>

<script>
(function() {
  const nav     = document.getElementById('sidebarNav');
  const toggle  = document.getElementById('sidebarToggle');
  const overlay = document.getElementById('sidebarOverlay');
  const STORAGE_KEY = 'rentit_sidebar_collapsed';

  // Collapse sidebar immediately (no flicker) — nav is already in DOM
  const isInitiallyCollapsed = localStorage.getItem(STORAGE_KEY) !== 'false';
  if (isInitiallyCollapsed) nav.classList.add('collapsed');

  // #pageContent is rendered AFTER this sidebar script, so wait for full DOM
  document.addEventListener('DOMContentLoaded', () => {
    const content = document.getElementById('pageContent');

    // Sync content margin with saved state
    if (isInitiallyCollapsed && content) content.classList.add('collapsed');

    toggle.addEventListener('click', () => {
      const nowCollapsed = nav.classList.toggle('collapsed');
      if (content) content.classList.toggle('collapsed', nowCollapsed);
      localStorage.setItem(STORAGE_KEY, nowCollapsed);
    });
  });
})();
</script>
